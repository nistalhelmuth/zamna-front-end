import { combineReducers } from 'redux';
import * as types from '../types/book';

/*
const user = (state= {}, action) => {
  switch(action.type) {
  case types.BOOK_FETCHED: {
    return
  }
  }
};

export default combineReducers({

});*/